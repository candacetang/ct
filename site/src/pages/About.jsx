import { aboutData, asset } from '../data/siteData';
export default function About() { return <main className="about"><div className="about-image"><img src={asset(aboutData.image)} alt="Illustration of Candace Tang eating noodles" /></div><div className="about-copy">{aboutData.bio.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}<blockquote>{aboutData.quote}</blockquote><address>{aboutData.contacts.map(({ label, text, href }) => <p key={label}><strong>{label}</strong> <a href={href} target={href.startsWith('http') ? '_blank' : undefined} rel={href.startsWith('http') ? 'noreferrer' : undefined}>{text}</a></p>)}</address></div></main>; }
